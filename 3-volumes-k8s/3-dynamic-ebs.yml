#PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dynamic-ebs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "dynamic-ebs-sc"
  resources:
    requests:
      storage: 3Gi
---
#POD
apiVersion: v1
kind: Pod
metadata:
  name: dynamic-ebs-app
  labels:
    purpose: roboshop-dynamic
spec:
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1d   #based on your nodes az, replace here
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: storage-volume
          mountPath: /usr/share/nginx/html
  volumes:
    - name: storage-volume
      persistentVolumeClaim:
        claimName: dynamic-ebs-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: dynamic-ebs-svc
spec:
  selector:
    purpose: roboshop-dynamic
  ports:
  - name: lb-svc-nginx
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30007
  type: LoadBalancer