apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: expense_prod
  region: us-east-1
managedNodeGroups:
  - name: prod_nodes
    instanceType: m5.large
    desiredCapacity: 3
    spot: true