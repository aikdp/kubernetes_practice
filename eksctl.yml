apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: expense-prod
  region: us-east-1
managedNodeGroups:
  - name: prod-nodes
    instanceType: m5.large
    desiredCapacity: 3
    spot: true