apiVersion: v1
kind: Service
metadata:
  name: lb-nginx
spec:
  selector:
    app: frontend
    project: robo
    envronment: prod
  ports:
  - name: lb-svc-nginx
    protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer