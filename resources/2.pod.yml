apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    project: expense
    app: nginx
    env: dev
spec:
  containers:
  - name: nginx
    # image: nginx:1.14.2
    image: nginx
    ports:
    - containerPort: 80