apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: expense
spec:
  selector:
    project: expense
    module: backend
    env: dev
  ports:
  - name: backend-port
    protocol: TCP
    port: 8080 #service port
    targetPort: 8080  #container port(app port)